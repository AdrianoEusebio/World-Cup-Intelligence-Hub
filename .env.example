# Configurações do Banco de Dados PostgreSQL (Docker)
DB_HOST=localhost
DB_PORT=5432

# Configurações do Banco de Dados PostgreSQL (Docker)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=worldcup
DB_USER=postgres
DB_PASSWORD=suasenha_aqui

# Integração com LLM (Qualquer provedor compatível com OpenAI)
# Se deixada vazia, a aplicação ativará o MockLLMClient automaticamente.
LLM_API_KEY=sua_api_key
LLM_API_BASE_URL=base_url
LLM_MODEL=modelo_da_llm

# Configurações de Ambiente
ENVIRONMENT=development

# API de Futebol (Football-Data.org)
FOOTBALL_API_KEY=chave_api_fotebol
